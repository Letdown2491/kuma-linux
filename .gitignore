/target
/Containerfile
# vm, vm-cosmic, and any future per-desktop disk directories
/vm*
/.claude

# Build outputs and the real declaration stay local — kuma.toml.example
# is the committed one.
/iso
/examples/kuma.toml
