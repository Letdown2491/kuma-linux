/target
/Containerfile
# vm, vm-cosmic, and any future per-desktop disk directories
/vm*
/.claude

# Build outputs and the real declarations stay local — the *.example
# files are the committed ones.
/iso
/examples/kuma.toml
/examples/*.kuma.toml
